{{- $myEmail := "shadkaske@gmail.com" -}}
{{- if eq .chezmoi.osRelease.id "fedora" -}}
  {{- $myEmail = "Shad.Kaske@showplacecabinetry.com" -}}
{{- end -}}
[data]
  email = {{ $myEmail | quote }}

[git]
  autoCommit = false
  autoPush = false

[merge]
    command = "nvim"
    args = [
        "-d",
        {{ printf "%q" "{{ .Destination }}" }},
        {{ printf "%q" "{{ .Source }}" }},
        {{ printf "%q" "{{ .Target }}" }},
    ]
