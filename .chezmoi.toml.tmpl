{{- $myEmail := "shadkaske@gmail.com" -}}
{{- if eq .chezmoi.osRelease.id "fedora" -}}
  {{- $myEmail = "Shad.Kaske@showplacecabinetry.com" -}}
{{- end -}}
[data]
  email = {{ $myEmail | quote }}
  firewalld_active = {{ output "systemctl" "is-active" "firewalld.service" | trim | quote }}

[git]
  autoCommit = false
  autoPush = false
